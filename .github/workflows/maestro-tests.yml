name: Maestro Tests
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  maestro:
    name: Run Maestro Flows
    runs-on: macos-14  # ← macos-latest yerine macos-14
    
    steps:
    - name: Checkout Code
      uses: actions/checkout@v4
      with:
        lfs: false
    
    - name: Configure Git
      run: |
        git config core.autocrlf false
        git config core.eol lf
    
    - name: Select Xcode 15.0.1
      run: sudo xcode-select -s /Applications/Xcode_15.0.1.app  # ← 15.0 yerine 15.0.1
    
    - name: Show Xcode Version
      run: xcodebuild -version
    
    - name: Build App
      run: |
        xcodebuild build-for-testing \
          -scheme userhub \
          -destination 'platform=iOS Simulator,name=iPhone 15,OS=17.0'
    
    - name: Install Maestro
      run: |
        brew tap mobile-dev-inc/tap
        brew install maestro
    
    - name: Start Simulator
      run: |
        xcrun simctl boot "iPhone 15" || true
    
    - name: Run Maestro Tests
      run: maestro test .maestro/flows/
    
    - name: Upload Screenshots
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        name: maestro-screenshots-${{ github.run_number }}
        path: ~/.maestro/tests/**/*.png
        if-no-files-found: ignore
